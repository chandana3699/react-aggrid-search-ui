export const API_URL = "https://jsonplaceholder.typicode.com/comments"; // Replace with actual API

export const DEFAULT_PAGE_SIZE = 10; // Default page size for pagination

export const PAGE_SIZE_OPTIONS = [5, 10, 20, 50]; // Available options for page size selection
